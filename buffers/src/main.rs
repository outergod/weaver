//! `weaver-buffers` binary entry point.
//!
//! Slice 003 Phase 1 scaffold: prints a marker string and exits. The
//! real CLI — clap-derived positional `<PATH>...` arguments, poll-
//! interval flag, socket override, tracing setup — lands in Phase 3
//! (task T039) per `specs/003-buffer-service/tasks.md`.

fn main() {
    println!("weaver-buffers (slice 003 scaffold)");
}
